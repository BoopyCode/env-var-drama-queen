# ENV Var Drama Queen 🎭

Prevents silent environment variable failures by checking them at startup and throwing a dramatic error if any are missing or empty. Run: `node env-drama-queen.js VAR1 VAR2 VAR3`